# Type Check

Run type checking for frontend and backend.

## Frontend

```bash
cd frontend && bun typecheck
```

## Backend

```bash
cd backend && go build ./...
```

Fix any errors before committing.
